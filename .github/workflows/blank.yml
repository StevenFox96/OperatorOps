name: OperatorOps setup

on: [push]

jobs:
  deploy_dWeb_smart_contracts:

    runs-on: ubuntu-latest

    steps:
    - name: Deploying smart contracts to EOS
      env: 
        EOS_PRIVATE_KEY: ${{ secrets.EOS_PRIVATE_KEY }}
      run: echo "EOS PRV KEY is $EOS_PRIVATE_KEY"
